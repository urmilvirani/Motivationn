import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Editloader = () => {
    return (
        <View style={{ width: '100%', alignItems: "center", position: "absolute", marginTop: 90 }}>

            <View style={{ backgroundColor: '#EAEAEA', height: 150, width: 150, borderRadius: 80 }} />
        </View>
    )
}

export default Editloader

const styles = StyleSheet.create({})